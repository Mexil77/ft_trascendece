{
	"name": "user-service",
	"version": "1.0.0",
	"description": "",
	"main": "./src/server.js",
	"type": "module",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev": "concurrently \"npm:server:dev\"",
		"watch:tsc": "tsc --watch",
		"server:dev": "nodemon --legacy-watch src --ext ts,js,json --exec \"node dist/server.js\""
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"@fastify/cors": "^11.0.1",
		"@punkish/fastify-better-sqlite3": "^2.0.0",
		"@types/node": "^24.0.1",
		"better-sqlite3": "^12.1.1",
		"fastify": "^5.4.0",
		"fastify-plugin": "^5.0.1"
	},
	"devDependencies": {
		"@types/better-sqlite3": "^7.6.13",
		"concurrently": "^9.1.2",
		"nodemon": "^3.1.10",
		"typescript": "^5.8.3"
	}
}
