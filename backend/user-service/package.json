{
	"name": "user-service",
	"version": "1.0.0",
	"description": "",
	"main": "./src/server.js",
	"type": "module",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev": "concurrently \"npm:watch:tsc\" \"npm:server:dev\"",
		"watch:tsc": "tsc --watch",
		"server:dev": "nodemon --watch src --ext ts,js,json --exec \"node dist/server.js\""
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"@fastify/cors": "^11.0.1",
		"@types/node": "^24.0.1",
		"fastify": "^5.4.0"
	},
	"devDependencies": {
		"concurrently": "^9.1.2",
		"nodemon": "^3.1.10",
		"typescript": "^5.8.3"
	}
}
